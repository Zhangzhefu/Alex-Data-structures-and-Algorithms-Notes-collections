# To sort with python's built-in function sorted():
# sorted(unsorted, key=lambda x: (len(x), x))

# this means that we first sort by its length, then we sort by its  actually size within that length
