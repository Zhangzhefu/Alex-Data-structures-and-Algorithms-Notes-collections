# all()
# This function checks if the given iterable has any None or empty elements.
#
# This function can also be used to check if all elements in one variable are presented in the other variable:

# all(elem in i for elem in j)
# -- this checks if all the elements in the j(second variable) are presented in i(the first variable)
