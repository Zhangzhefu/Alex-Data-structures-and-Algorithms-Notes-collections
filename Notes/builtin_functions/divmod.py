"""The divmod() function in Python takes two numbers as arguments and returns a tuple containing the quotient and
remainder when the first number is divided by the second. It's equivalent to doing

quotient = num // divisor
remainder = num % divisor

"""

result = divmod(10, 3)
print(result)
